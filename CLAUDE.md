# CLAUDE.md

See **AGENTS.md** for the full guide: dev environment, project structure, commands, backend architecture, schema workflow, planning docs, and dev logs.

## Quick reminders

- Always run `pnpm run check-types` after edits.
- Run commands via `nix develop --command …` (see AGENTS.md).
- Planning docs → `agents/plans/YYYY-MM-DD-<slug>.md` (ADR format).
- General specs → `docs/` (new-developer guides, kept high-level).
